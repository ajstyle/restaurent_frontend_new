import { SelectedCssDirective } from './selected-css.directive';

describe('SelectedCssDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedCssDirective();
    expect(directive).toBeTruthy();
  });
});
